---
title: DNS / nslookup
description: Consulta registros DNS (A, AAAA, MX, TXT, NS, CNAME, SOA, CAA, SRV, PTR) via DNS-over-HTTPS.
---

# DNS / nslookup

Consulta registros DNS de um domínio (ou PTR reverso de um IP): **A, AAAA, MX, TXT, NS, CNAME, SOA, CAA, SRV, PTR** — via DNS-over-HTTPS.

<ClientOnly>
  <NetTools only="dns" />
</ClientOnly>
